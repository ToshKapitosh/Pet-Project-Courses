import styled from 'styled-components';

export const StyledCourses = styled.main`
	display: flex;
	flex-direction: column;
	gap: 40px;
	padding: 20px;
	border: 4px solid blue;
`;

export const StyledHeadingCourses = styled.div`
	display: flex;
	justify-content: space-between;
`;
